# enterprise.powerhouse.io — Site Outline v3

**Primary audience:** The CTO / VP of Engineering whose board is demanding an AI strategy — and who knows that waiting for the "safe choice" means being late. Not the risk-averse buyer who needs 10 reference clients. The one who sees being first as competitive advantage.

**Secondary audiences:** CIO / CDO (scannable guardrails and proof point), Enterprise Architect (technical depth, links to vetra.io).

**Site role:** Technology hub that routes to bai.powerhouse.io (assessment & consulting), vetra.io (platform, packages, hosting, agents), and achra.com (builders & operators marketplace).

**Positioning:** Frontier infrastructure for AI-native operations. Not a mature vendor with 500 logos — a co-creation partner for enterprises that want to lead.

**Narrative arc:** Urgency -> Caution -> The real blocker -> The solution -> Guardrails -> How it works -> Why us (frontier + co-creation) -> Get moving.

See [emotional-drivers.md](emotional-drivers.md) for the concern hierarchy.
See [vendor-checklist.md](vendor-checklist.md) for the reliability assessment this version addresses.

---

## 1. Hero

**Headline:** "Frontier Infrastructure for AI-Native Business Operations"

**Subhead:** Your board wants an AI strategy. The safe choices are slow, locked-in, and built on architectures that weren't designed for machines. Powerhouse is the alternative: a local-first document platform that makes your business data AI-readable from day one — open source, on your infrastructure, under your control.

**CTA:** "Request a Demo" (primary) / "See How It Works" (scroll)

*"Frontier" sets expectations honestly: this is cutting-edge, not commodity. "Request a Demo" is the primary CTA because the demo — showing Achra and Vetra running on the stack — is where conviction happens. The architecture page builds interest; the demo closes it.*

---

## 2. The Real Blocker

**"AI doesn't fail because of models. It fails because of data."**

Most enterprise AI initiatives stall at the same point: the data isn't ready. Business logic is buried in ERP modules. Operational knowledge lives in people's heads and spreadsheets. Data is scattered across proprietary formats that no AI agent can read, let alone act on.

The result: companies bolt chatbots onto systems that were never designed for machine interaction — and get nothing back.

The missing piece isn't a better model. It's a structured data layer that makes your business processes readable, writable, and verifiable — for humans and machines alike.

*Names driver 3. The CTO who has already tried an AI pilot and watched it stall recognizes this immediately.*

---

## 3. The Solution: Documents as Data Primitives

**"One data abstraction. Every business object."**

Powerhouse replaces the unstructured mess with a single, principled data layer. A Powerhouse document is a typed, self-contained data structure defined by a **document model** — a schema that specifies its shape and the exact operations that can modify it. Under the hood, it's JSON. On the wire, it's an append-only sequence of signed operations.

**What this gives you:**

- **Schema-enforced consistency** — Operations have well-defined parameters and built-in business logic. The document state is always valid by construction, not by validation after the fact.
- **AI-native by default** — Every document model automatically generates GraphQL and MCP endpoints. The same operations available to human editors are available to AI agents — no adapter layer, no integration project.
- **Cryptographic verification** — Every operation is signed by its author. The full history is offline-verifiable without trusting a central authority.
- **Git-like version control** — Branching, merging, undo, and history traversal come free from the append-only model. No additional development needed.
- **Storage-agnostic** — The same document can live as a file, in a browser, in PostgreSQL, or on-chain. Your choice of backend, same data guarantees.

---

## 4. The Guardrails

**"Move fast. Stay safe."**

Three scannable panels. Each names a concern the CTO has already raised internally.

### Your IP stays yours

Documents live on your infrastructure. Sensitive workflows run on local open-source models — your GPU, your network. General-purpose tasks use commercial APIs. Swap providers without architectural changes. Your data never trains a third-party model.

### No new lock-in

The entire stack is open source. Every component can be replaced independently. Your data is JSON. Your schemas are Zod definitions you own. If you leave, everything comes with you — no extraction project, no vendor negotiation.

### Auditable by architecture

Every operation — human or AI — is cryptographically signed, timestamped, and appended to an immutable history. The audit trail isn't a log bolted onto the side; it *is* the data structure. SOX, GDPR, industry-specific regulation: the compliance story is built into the primitive, not retrofitted.

*This is the section a CTO forwards to their legal and compliance teams.*

---

## 5. Architecture: How It Scales

**"Event-sourced. CQRS. Horizontally scalable. Local-first."**

The reactive document architecture turns documents from individual data structures into a platform-grade backend.

**Write path** — All mutations are document operations. Because documents are self-contained and independent, the write layer scales horizontally by adding nodes. There's no shared-state bottleneck; each document is its own consistency boundary.

**Read path** — Specialized processors subscribe to document update events and build denormalized read models. Store aggregated data wherever it fits: relational databases, search indexes, data warehouses, caches. Read and write concerns are fully separated (CQRS).

**Sync layer** — Documents synchronize across devices, apps, and users via their operation histories. Every document becomes a real-time collaboration channel. Every editor becomes a collaborative application — without custom sync code.

**Result:** The same architecture pattern that powers large-scale platforms — but local-first, open source, and running on your infrastructure.

---

## 6. The Platform

**"Six components. Each independent. Stronger together."**

### Connect

The operator workspace. A browser-based UI for creating, editing, and collaborating on business documents. **Drive apps** overlay the file system with specialized workflow interfaces — standardized processes directly available, rather than leaving operators to organize their own folder hierarchies.

### Switchboard

The API and integration layer. Exposes every document and operation as GraphQL queries/mutations and MCP tool endpoints. The system boundary where external applications, automation workflows, and AI agents interact with your document infrastructure. Handles authorization, sync, and multi-tenant access.

### Clint

AI-native command infrastructure. A single set of schema-based command definitions — derived directly from your document models — produces a CLI, an interactive REPL, an MCP server, and an AI agent harness. Operators use typed commands or natural language. Autonomous agents use the same operations through the routine loop — a continuous execution engine with pluggable triggers for event-driven, timer-based, or condition-based processing. One codebase, every interaction surface.

### Fusion

Custom platform builder. A Next.js-based framework for deploying branded, customer-facing applications on top of your document infrastructure. Switchboard-backed, fully customizable. Build the equivalent of a SaaS product — with the backend entirely in your hands.

### Renown

Identity and reputation layer. Verified contributor profiles and contribution tracking across your organization and partner networks. The identity substrate for authorization, audit trails, and cross-organizational collaboration.

### Vetra

The platform ecosystem. Three pillars:

- **Packages** — A registry of reusable document models, editors, read models, and agent configurations. Pre-built modules for common business domains. Use as-is, customize, or publish your own.
- **Hosting** — Managed cloud infrastructure with real-time sync. Production-ready deployment without hyperscaler dependency. Your data sovereignty preserved.
- **AI Agents** — Pre-configured business process agents built on the document architecture. Deploy from the ecosystem or build custom agents with Clint.

Development tooling (Vetra Studio IDE, PH-CLI), comprehensive documentation (Vetra Docs), and a learning platform (Vetra Academy) support teams building on the stack.

---

## 7. AI Integration

**"Native, Not Bolted On"**

Most enterprise AI integrations require building a translation layer between the AI and the business system. Powerhouse eliminates this: documents *are* the shared interface for humans and machines.

- Document models generate MCP endpoints automatically. Any MCP-compatible agent reads and writes business data through the same typed operations as human editors.
- Clint's **agent skills** are structured process playbooks — not open-ended prompts — that encode domain expertise as step-by-step workflows. Agents follow specifications, not guesses.
- The **routine loop** enables autonomous processing: agents monitor document changes, evaluate triggers, and execute operations on a continuous cycle — with or without human oversight.
- **Specification-driven AI**: collaboration with agents happens through specification documents. Fine-grained control where it matters, automation where it doesn't.

---

## 8. Technical Characteristics

**Scannable reference for architecture review.**

| Characteristic | Detail |
|---|---|
| **Data model** | Typed documents (JSON) with Zod schemas, GraphQL-based schema definition, custom scalars for business types (monetary amounts, identifiers, timestamps) |
| **Mutation model** | Append-only operation history; pure reducers (deterministic, no side effects); operation-level error handling |
| **Storage** | Pluggable adapters: filesystem, PostgreSQL, pglite (browser), in-memory, blockchain. Same document, any backend |
| **Sync** | Operation-based synchronization across devices, apps, and users. Conflict resolution via branch/merge semantics |
| **APIs** | GraphQL (Switchboard) + MCP (Switchboard & Clint). Auto-generated from document model schemas |
| **Auth** | Cryptographic operation signing; per-drive authorization; Renown identity integration |
| **Scalability** | Horizontal write scaling (independent consistency boundaries); CQRS read models; event-driven processors |
| **Stack** | TypeScript, React, GraphQL, Node.js 22+, Zod 4. Open source |
| **AI framework** | Mastra-based agent integration; provider-agnostic LLM backends; thread-based conversation memory; MCP tool discovery |
| **Compliance** | Immutable operation history; cryptographic signing; full provenance chain for every state change, human or AI |
| **Deployment** | Self-hosted or Vetra Cloud managed hosting |

---

## 9. How This Compares

| | Traditional ERP / SaaS | Powerhouse |
|---|---|---|
| **AI readiness** | Bolt-on integration, separate adapter per system | Every document is an MCP endpoint from day one |
| **Time to AI value** | Months of integration work before agents can act | Structure your data, agents work immediately |
| **Data location** | Vendor-hosted, proprietary formats | Local-first, open formats, your infrastructure |
| **Process changes** | Vendor engagement, months, regression risk | Update the document model schema, regenerate interfaces |
| **Audit trail** | Application-level logging, reconstructed after the fact | Cryptographically signed, append-only — the history *is* the data |
| **Vendor lock-in** | Multi-year contracts, proprietary data formats | Open source, storage-agnostic, modular components |
| **Scaling model** | Vertical (bigger servers, more expensive licenses) | Horizontal (document sharding, CQRS read models) |

---

## 10. Why Powerhouse

**"We build on what we ship."**

This section replaces v2's "Proven at Scale." It addresses the vendor reliability question head-on with the frontier co-creation framing.

### We run on our own stack

Powerhouse doesn't just sell this infrastructure — we run our business on it. **Achra**, our marketplace for builders and operators, and **Vetra**, our development and hosting platform, are both Powerhouse applications built on the same reactive document architecture, Connect workspace, Switchboard APIs, and Clint agent harnesses available to you. When we demo the platform, we're showing you our production systems.

### The stack is mainstream, the architecture is frontier

The underlying technology is TypeScript, React, GraphQL, Node.js, and Zod — your team already knows it. The reactive document layer on top is the innovation: a principled architecture for AI-native operations that doesn't exist elsewhere. Your engineers learn a pattern, not a new language.

### Early means advantage

This is frontier infrastructure. We're not pretending to have 500 enterprise logos. What we offer is something those vendors can't: direct access to the team that builds the platform, a co-creation partnership where your requirements shape the product, and a head start on infrastructure your competitors will adopt later. Early movers build in-house expertise before the talent market catches up.

### Support from the people who built it

Enterprise support is delivered through **BAI** (bai.powerhouse.io) with contractual SLAs — not a ticket queue, but a working relationship with the engineering team behind the platform. Implementation partners and operational service providers are available through the **Achra** network (achra.com). Early clients get the core team's attention. That's the advantage of being early.

### Open source is your insurance

If Powerhouse disappeared tomorrow, your data (JSON files), your document models (Zod schemas), and your application code (TypeScript) remain yours. The entire stack is open source. There is no proprietary format to extract from, no hosted service you can't replicate. Architectural independence is not a sales pitch — it's how the system works.

---

## 11. Get Started

**"Three ways to move."**

**See it live** — Request a demo of the Powerhouse platform. We'll show you Achra and Vetra — real applications running on the stack — and walk through how the architecture maps to your operations.
-> Request a Demo

**Assess your readiness** — Get an AI readiness assessment from the BAI team: data flow audit, readiness score, and a prioritized implementation roadmap.
-> bai.powerhouse.io

**Explore the platform** — Dive into the developer environment, package registry, documentation, and managed hosting.
-> vetra.io

**Find implementation partners** — Connect with builders and operational service providers in the Powerhouse network.
-> achra.com

**Or talk to us directly:** hello@powerhouse.inc

---

## Page-Level Design Notes

- **Single page, no subpages.** Depth lives on the linked sites.
- **"Request a Demo" is the primary CTA** — appears in hero and footer. The demo carries the conviction that reference clients would normally provide.
- **Architecture diagram** in section 5 is the primary visual — write/read/sync paths with component labels.
- **The guardrails section (4) should be visually distinct** — three equal panels, icon-led, scannable at a glance. Forwardable to legal/compliance.
- **Section 10 ("Why Powerhouse") is the trust section** — visually differentiated from the technology sections. More personal, less technical. This is where the reader decides if they trust the team, not just the tech.
- **Monochrome + one accent color.** Enterprise credibility, not startup energy.
- **Mobile: sections collapse** to headline + first sentence, expandable. Tables become card stacks.

---

## Structural Changes from v2

| Change | Rationale |
|---|---|
| Hero rewritten with "frontier" positioning | Sets honest expectations. This isn't a commodity purchase — it's an early-mover bet. Attracts the right CTO, filters out the wrong one. |
| "Request a Demo" becomes primary CTA | The demo (showing Achra/Vetra running on the stack) is the strongest proof point. It replaces the reference client call that doesn't exist yet. |
| "Proven at Scale" replaced by "Why Powerhouse" | v2's Sky/MakerDAO framing oversold production maturity. v3 leads with dogfooding (honest, demonstrable), mainstream stack (reduces perceived risk), early-mover advantage (reframes weakness as value), direct support (turns small team into an asset), and open-source insurance (architectural argument for longevity). |
| Vetra Docs added to platform section | Documentation is a prerequisite for CTO confidence. Positioned as real (forthcoming in the near term). |
| Demo CTA added to Get Started | "See it live" is the first option — before assess, build, or partner. The demo is the conversion event. |
| v2's comparison table rows preserved | "AI readiness" and "Time to AI value" still lead the table — urgency driver. |
| Technology sections (3–8) largely unchanged | The architecture content was already strong in v2. The structural change is what surrounds it: honest positioning before, trust-building after. |
